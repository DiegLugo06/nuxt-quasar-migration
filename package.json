{
  "name": "nuxt-quasar-migration",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "build:clean": "rimraf .nuxt .output node_modules/.cache && nuxt build",
    "build:production": "NODE_ENV=production nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "clean": "rimraf .nuxt .output node_modules/.cache .npm",
    "test": "pytest tests/ -v -s",
    "test:quote": "pytest tests/test_quote_flow.py -v -s"
  },
  "dependencies": {
    "@pinia/nuxt": "^0.10.1",
    "@quasar/extras": "^1.16.4",
    "axios": "^1.7.7",
    "nuxt": "^3.13.2",
    "pinia": "^3.0.1",
    "pinia-plugin-persistedstate": "^4.2.0",
    "quasar": "^2.15.0",
    "vue": "latest",
    "vue-router": "latest"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.14.0",
    "http-proxy-middleware": "^3.0.5",
    "rimraf": "^5.0.5",
    "sass-embedded": "^1.93.3",
    "tailwindcss": "^3.4.18"
  }
}
